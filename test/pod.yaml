apiVersion: v1
kind: Pod
metadata:
  name: security-context-demo
spec:
  securityContext:
    runAsUser: 1000
    runAsGroup: 3000
    # runAsNonRoot
    # seLinuxOptions
    # seccompProfile
    # windowsOptions
    fsGroup: 2000
    # fsGroupChangePolicy
    # supplementalGroups
    # sysctls
  volumes:
  - name: sec-ctx-vol
    emptyDir: {}
  containers:
  - name: sec-ctx-demo
    image: busybox:1.28
    command: [ "sh", "-c", "sleep 1h" ]
    volumeMounts:
    - name: sec-ctx-vol
      mountPath: /data/demo
    securityContext:
      # runAsUser:
      # runAsGroup:
      # runAsNonRoot:
      # seLinuxOptions:
      # seccompProfile:
      # windowsOptions:
      allowPrivilegeEscalation: false
      # capabilities:
      # privileged:
      # procMount:
      # readOnlyRootFilesystem: