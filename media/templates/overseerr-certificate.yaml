apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: overseerr-certificate
spec:
  secretName: overseerr-certificate-key-pair
  isCA: true
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
  commonName: {{ .Values.overseerr.ingress.host }}
  emailAddresses:
  - "marcinlagowski92@gmail.com"
  subject:
    organizations:
      - "emlagowski.github.io"
    countries: 
      - "PL"
    provinces: 
      - "Mazowieckie"
    localities: 
      - "Warsaw"
  privateKey:
    algorithm: "ECDSA"
    size: 256
    rotationPolicy: "Always"
  dnsNames:
  - {{ .Values.overseerr.ingress.host }}
  secretTemplate:
    annotations:
      kubed.appscode.com/sync: "kubernetes.io/metadata.name=istio-system"