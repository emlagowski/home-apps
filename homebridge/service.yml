apiVersion: v1
kind: Service
metadata:
  name: homebridge
  namespace: homebridge
spec:
  selector:
    app.kubernetes.io/instance: homebridge
  ports:
    - name: http
      port: 8090
      targetPort: 8090
      protocol: TCP