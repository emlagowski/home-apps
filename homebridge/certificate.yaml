apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homebridge-certificate
spec:
  secretName: homebridge-certificate-key-pair
  isCA: true
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
  commonName: "homebridge.raspberrypi"
  emailAddresses:
  - "marcinlagowski92@gmail.com"
  subject:
    organizations:
      - "emlagowski.github.io"
    countries: 
      - "PL"
    provinces: 
      - "Mazowieckie"
    localities: 
      - "Warsaw"
  privateKey:
    algorithm: "ECDSA"
    size: 256
    rotationPolicy: "Always"
  dnsNames:
  - "homebridge.raspberrypi"
  secretTemplate:
    annotations:
      kubed.appscode.com/sync: "kubernetes.io/metadata.name=istio-system"